const index = require('../index');
const fs = require('fs');

function generateTeam(team) {
    //placeholder until further functionality has been added
    console.log('Team built!');
}

module.exports = generateTeam;