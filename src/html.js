const index = require('../index');
const fs = require('fs');

function generateHTML(team) {
    //placeholder until further functionality has been added
    //Will generate html using a template literal with inputs dynamically appended
    console.log('Team built!');
}

module.exports = generateHTML;